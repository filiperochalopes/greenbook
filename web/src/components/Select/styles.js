import styled from "styled-components";

export default styled.div`
  max-width: 200px;
  margin-right: 4px;

  p {
    font-size: 0.7rem;
  }

  span {
    color: red;
  }

  span.error {
    display: block;
    color: red;
    font-size: 0.8rem;
  }
`;
