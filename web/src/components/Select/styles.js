import styled from "styled-components";

export default styled.div`
display: inline-block;
  margin-right: 4px;

  p {
    font-size: 0.7rem;
  }

  span {
    color: red;
  }

  span.error {
    display: block;
    color: red;
    font-size: 0.8rem;
  }
`;